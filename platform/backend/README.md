# Demonstration platform backend

API, authentication, post/comment/reaction services, model inference, and
analytics aggregation belong here. Keep research training code in the root
Python package so the platform only loads a versioned exported model.

