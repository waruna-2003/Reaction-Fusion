# Pipeline scripts

Keep thin command-line entry points here for collection import, validation,
preprocessing, label generation, splitting, training, and evaluation. Reusable
logic belongs in `src/reactionfusion/` rather than notebooks or scripts.

