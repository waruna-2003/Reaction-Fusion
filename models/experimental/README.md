# Historical experimental models

These serialized JSON models are frozen snapshots from the ReactionFusion V2–V5
development experiments. They are retained for reproducibility and presentation
of the model-evolution process, not for production deployment or final benchmarking.

The datasets used to produce them are no longer part of the active repository.
Supporting configurations and evaluation evidence are available in
`experiments/model_evolution/`.
